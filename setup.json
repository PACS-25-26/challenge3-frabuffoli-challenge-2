{
    "n"          : 32,
    "f"          : "8*pi^2*sin(2*pi*x)*sin(2*pi*y)",
    "solver"     : "jacobi",
    "exact"      : "sin(2*pi*x)*sin(2*pi*y)",
    "tol"        : 1e-5,
    "max_iter"   : 50000,
    "omp_threads": 2
}
